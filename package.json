{
  "$schema": "https://json.schemastore.org/package",
  "name": "pw-typescript",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "formslayouttest": "npx playwright test formsLayoutTest.spec.ts --project=chromium",
    "navigationtest": "npx playwright test navigationTest.spec.ts --project=chromium",
    "formsAndNavigationSequential": "npx playwright test formsLayoutTest.spec.ts --project=chromium && npx playwright test navigationTest.spec.ts --project=chromium",
    "formsAndNavigationParallel": "npx playwright test formsLayoutTest.spec.ts --project=chromium & npx playwright test navigationTest.spec.ts --project=chromium",
    "setup": "setup.bat",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d angular-app",
    "docker:test": "docker-compose run --rm playwright-tests",
    "docker:test-and-cleanup": "docker-compose up -d angular-app && docker-compose run --rm playwright-tests && docker-compose down",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs angular-app",
    "pageObjects-chrome": "npx playwright test --project=chromium",
    "smoke-tests": "npx playwright test tests/formsLayoutTest.spec.ts --grep @smoke --project=docker-chrome",
    "smoke-tests-local": "npx playwright test tests/formsLayoutTest.spec.ts --grep @smoke --project=chromium",
    "docker:smoke-test": "docker-compose run --rm playwright-tests npm run smoke-tests",
    "docker:smoke-and-cleanup": "docker-compose up -d angular-app && docker-compose run --rm playwright-tests npm run smoke-tests && docker-compose down",
    "docker:all-tests": "docker-compose run --rm playwright-tests npm run pageObjects-chrome",
    "docker:firefox-tests": "docker-compose run --rm playwright-tests npx playwright test --project=firefox"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@faker-js/faker": "^9.9.0",
    "@playwright/test": "^1.54.1",
    "@types/dotenv": "^6.1.1",
    "@types/node": "^24.1.0",
    "allure-commandline": "^2.34.1",
    "allure-playwright": "^3.3.3"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "oracledb": "^6.9.0"
  }
}
